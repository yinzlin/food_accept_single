一库双账设计已完成实现。以下是详细说明：

## 一、数据模型（一库双账）

**真实账套**（不动）：
- `sales_order` / `sales_order_item` —— 原始真实单据，永不修改

**分摊账套**（独立表）：
- `consumable_allocation` —— 分摊方案主表（总金额、已分摊、未分摊、状态）
- `order_supplement_item` —— 增项流水表，记录两种操作：
  - `operation_type = 'new_item'`：新增商品行（有独立的 `supplement_id`）
  - `operation_type = 'increase_quantity'`：追加已有商品数量（通过 `target_order_item_id` 关联真实明细行，多条记录可累计合并）

**关键设计**：同一真实商品的多次"追加数量"在 `order_supplement_item` 中是多条流水，但在**展示分摊账套时合并为一条**，保证"只保持一条记录"的视觉效果。

## 二、新增 API

**`GET /api/supplement/compare/{order_id}`** ([main.rs:15313](file:///d:/projects/food_accept_single/src/main.rs#L15313-L15469))

返回目标订单的真实账套与分摊账套对比数据：
```json
{
  "real_total": 1000.00,        // 真实金额
  "supplement_total": 280.00,   // 增项总金额
  "allocation_total": 1280.00,  // 分摊后总金额
  "items": [
    {
      "id": 123,
      "product_name": "猪肉",
      "is_increase": true,       // 数量被追加
      "is_new": false,
      "quantity": 20,           // 真实数量
      "amount": 600,            // 真实金额
      "supplement_quantity": 2.6, // 追加数量
      "supplement_amount": 78,    // 追加金额
      "total_quantity": 22.6,     // 分摊后总数量
      "total_amount": 678         // 分摊后总金额
    },
    {
      "is_new": true,             // 新增商品
      "total_quantity": 10,
      "total_amount": 202
    }
  ]
}
```

## 三、前端页面改造

### 1. 左右并排对比视图 ([main.rs:14486-14517](file:///d:/projects/food_accept_single/src/main.rs#L14486-L14517))

选择目标订单后，显示**真实账套 vs 分摊账套**并排对比：

| 真实账套（左） | 分摊账套（右） |
|---------------|---------------|
| 灰色背景 | 绿色边框 + 绿色表头 |
| 普通商品行 | 普通商品行（无差异） |
| — （空位） | **黄色背景 + [增项] 标签**（新增商品行） |
| 原始数量/金额 | **绿色背景 + +N badge**（追加数量商品） |

顶部显示差异金额徽章：`差异 +280.00`

### 2. 增项操作重构 ([main.rs:14518-14590](file:///d:/projects/food_accept_single/src/main.rs#L14518-L14590))

两种操作模式：

**新增商品行**：
- 点击输入框从商品库选择（自动过滤"耗材"类别）
- 输入数量 + 单价 → 自动计算金额
- 数量×单价 = 分摊金额

**追加已有商品数量**：
- 下拉选择目标订单中的非耗材商品
- 输入追加数量 → 自动计算金额和合计数量
- 例：真实20 + 追加2.6 = 合计22.6，**只显示一条记录**

### 3. 实时预览

添加待保存的增项后，分摊账套表实时更新预览效果，保存后从服务器重新拉取正式数据。

## 四、业务规则

- 真实账套与分摊账套查询统计**完全隔离**：真实订单查询走 `sales_order_item`，分摊对比走 `order_supplement_item` 合并
- 增项金额受分摊方案"未分摊余额"限制，不能超出
- 状态管理：未初始化 / 未分摊 / 分摊中 / 已完成 / 已终止

服务器已启动运行，您可以刷新浏览器进入"耗材分摊管理"页面测试。选择采购单位 → 选择耗材订单 → 初始化分摊方案 → 选择目标订单 → 即可看到左右并排的真实账套与分摊账套对比，并进行增项操作。