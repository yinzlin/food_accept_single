# 销售订单生成采购订单（补充式合并）功能描述

> 本文档描述当前系统"通过销售订单生成采购订单"功能的完整业务逻辑，可作为复现、继续开发或审查该功能的依据。
> 核心实现：`src/api/mod.rs` → `api_sales_order_generate_purchase`（L6392-7097）；前端：`src/pages/mod.rs` → `generatePurchaseOrders`。

## 一、入口与权限

- **前端**：销售订单页面（`/sales`）列表每行的「生成采购订单」按钮，调用 `POST /api/sales_order/generate_purchase/{销售单id}?force={0|1}`。
- **权限点**：`sales_order.create`（权限映射中 `generate_purchase` 归入 create）。
- **行级数据权限**：`super_admin`/`admin` 可操作全部订单；`purchaser` 仅可操作自己绑定的采购单位的订单（`can_access_sales_order`）。
- 销售单不存在返回 404；无权限返回 403。

## 二、重复生成检测（决定走「直接返回 / 警告 / force 重算」三态）

任一条件命中即视为"本销售单已生成过采购订单"，三条检测路径：

1. 采购订单主表 `purchase_order.source_sales_order_id = 本销售单id`（首次创建时写入的来源）；
2. 采购明细 `purchase_order_item.source_sales_order_id = 本销售单id`（严格限定本单，避免误伤其他销售单或手动添加的明细）；
3. 兜底：本销售单所有明细的 `supplier_id` 集合中，当天（`order_date` 相同）已存在**非 cancelled** 状态的同供应商 PO（覆盖"明细合并进他人创建的 PO"的场景）。

检测结果分两类：

- **存在已处理单**（非 pending，即已进入分拣/配送/验收/结算等流程）：直接返回 `error`，提示"该销售订单已生成过采购订单，其中 N 张已处理（每张换行显示：`单号（供应商，状态）`），不能重新生成，需到采购订单页面手动处理"。
- **存在 pending 单且非 force**：返回 `warning`（含 count 和换行明细列表 `单号（供应商）`），前端 `confirm` 后自动以 `force=1` 重发。
- **force=1**：进入下方"补充式"重算流程。

## 三、force=true 补充式重算流程

### 前置准备（只影响"本销售单已生成的 PO"）

1. 收集受影响 PO 集合：检测到的 pending PO + 当天同供应商非 cancelled PO（按 `order_date` + 本单涉及 `supplier_id` 过滤）。
2. 对每个受影响 PO，取其中 `source_sales_order_id = 本单` 的旧明细，暂存为 `old_items_by_po`。
3. 读取本销售单 `sales_order.generated_purchase_item_ids` **快照**（JSON 数组，存 PO 明细主键 id），翻译成 `(product_id, unit_key, warehouse_id) → po_item_id` 索引（`unit_key` 取 `sales_unit` 非空则用 sales_unit，否则用 `unit`）。索引键必须含 warehouse_id。

### 销售明细准备

- 查询 `sales_order_item` JOIN `product`，跳过 `supplier_id = 0` 的明细。
- 单价规则：`unit_price = purchase_price > 0 ? purchase_price : base_price`（进价优先，否则商品基础价）。
- `ordered_quantity = pre_sale_quantity`（销售单预售数量，缺省用 quantity）；`amount = quantity × unit_price`。
- 按 `supplier_id` 分组。若无任何有供应商的明细 → 返回 400「销售订单中没有供应商信息，无法生成采购订单」。

### 按供应商逐个处理

- 目标 PO：查当天（`order_date`）同供应商**非 cancelled** 的 PO（取 id 最小者）→ 存在则**合并**到该 PO；不存在则**新建 PO**：
  - 单号 `generate_order_no("purchase", order_date)`：`PO` + `yyyyMMdd` + 当日该表最大 3 位序号 +1；
  - 主表写入：`supplier_id`、`order_no`、`order_date`、金额四项均初始化为 0、`warehouse_id`/`warehouse_name` = 销售单主表仓库、`remark` 空、`source_sales_order_id` = 本单。

- **明细"补充式"CRUD**（对每条销售明细，匹配键为三元组 `(product_id, sales_unit, 销售单主表 warehouse_id)`）：

  1. **键命中快照**：按快照中 PO 明细主键 `UPDATE`，同步商品信息/单价/数量/金额/ordered_quantity/备注/仓库/sales_unit；金额差量 `delta += 新amount - 旧amount`。若 UPDATE 影响 0 行（该明细已被用户从 PO 删除）→ 退化为分支 2/3 补回。
  2. **键未命中快照，但 PO 中存在同 `(product_id, unit_key, warehouse_id)` 的"孤儿"明细**（`source_sales_order_id` 为其他值，典型为老代码遗留的主表兜底孤儿；优先选 `source=本单`，否则取 id 最小）：`UPDATE` 复用该行，同步字段**并把 `source_sales_order_id` 改为本单**，金额按差量处理。这是防止"孤儿 + 新插入"并存产生 (P,U) 重复的关键，且**匹配必须含 warehouse_id**——不同仓库出库的相同商品是不同行，必须同时保留，不得跨仓库复用。
  3. **均未命中**：`INSERT` 新明细（`source_sales_order_id = 本单`），金额全量计入 delta。

- 快照中有但销售明细不再出现的键：仅从快照清除（PO 对应行**保留不删**）；PO 中 `source=本单` 但不在新快照的明细也**保留不动**（防误删）。
- 主表金额：`delta` 非零时，`total_amount` 与 `final_amount` 同步加减。
- 合并计数：`merged_count += UPDATE数 + INSERT数`。

### 收尾

- 将本轮实际写入/更新的 PO 明细主键 id 集合排序后 JSON 回写 `sales_order.generated_purchase_item_ids`。
- 写审计日志：`sales_order.generate_purchase` → 「由销售单生成采购订单，新建 X 张，合并明细 Y 条」。
- 返回 `{count, merged, message}`：
  - count>0 且 merged>0：「成功生成 X 张采购订单，并合并 Y 条相同明细」；
  - count>0：「成功生成 X 张采购订单」；
  - 否则：「已将销售明细合并到已有采购订单（Y 条合并）」。
- 成功响应后前端刷新销售单列表。

## 四、关键数据表与字段

- `sales_order`：`order_no`（SO+日期+3位序号）、`purchaser_id`、`order_date`、`warehouse_id/warehouse_name`（出库仓库，生成 PO 的仓库来源）、`status`（pending→confirmed→sorting→sorted→delivering→delivered→accepted→settled，另有 cancelled）、`generated_purchase_item_ids`（JSON 快照）、`version`、金额四件套。
- `sales_order_item`：`product_id/product_name/alias1/alias2/spec/unit/quantity`、`pre_sale_quantity`（→PO 的 ordered_quantity）、`supplier_id/supplier_name`、`remark`。
- `purchase_order`：`supplier_id`、`order_no`（PO+日期+3位序号）、`order_date`、`total_amount/discount_rate/amount_reduction/final_amount`、`warehouse_id/warehouse_name`、`status`、`source_sales_order_id`（主表来源，唯一标识首建单）。
- `purchase_order_item`：`order_id`、商品信息、`unit_price/quantity/base_quantity/amount/ordered_quantity`、`warehouse_id/warehouse_name`、`sales_unit`（销售单位，与 unit 区分）、`source_sales_order_id`（明细归属来源）。

## 五、必须遵守的业务规则（复现要点）

1. **不重复**：已生成的 PO 靠「主表 source + 明细 source + 当天同供应商兜底」三路检测；用户删除 PO 明细后重新 force，靠快照驱动按主键 UPDATE，id 消失才补插。
2. **孤儿复用**：快照外的 (P,U,仓库) 优先复用老代码遗留孤儿并改写 source，杜绝 (P,U) 重复行。
3. **仓库为行级维度**：匹配键 `(product_id, unit_key, warehouse_id)` 三元组；不同仓库出库同商品在同一个 PO 中必须同时保留，禁止跨仓库复用/覆盖。
4. **不误删他人**：只增删改 `source_sales_order_id = 本单` 的明细；其他销售单贡献的明细不受影响；"补充式"不删除主表 PO。
5. **金额一致**：主表金额按差量（delta）调整，始终等于明细 `quantity × unit_price` 之和（忽略折扣）。
